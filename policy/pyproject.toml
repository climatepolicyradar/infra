[project]
name = "infra-policy"
version = "1.0.0"
requires-python = ">=3.12,<3.14"
dependencies = ["pulumi-policy>=1.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["policies"]
